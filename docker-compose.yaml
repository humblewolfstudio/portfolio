version: "3.3"

services:
  spa-to-http:
    build: "Dockerfile"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portfolio.rule=Host(`portoflio.capicua.org.es`)"
      - "traefik.http.services.portfolio.loadbalancer.server.port=8080"